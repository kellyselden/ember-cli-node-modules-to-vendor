(function() {
   function testFunc() {
     return 'The funnel works!';
   }
   if (typeof define === 'function' && define.amd) {
     define([], function() { return testFunc; });
   } else {
     throw new Error('No amd loader found');
   }
 })();
